#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main() {
    
    // const float PI = 3.14;
    // const float g = 9.8;
    
    ll x ;
    ll y ;
    int sum;
    cin >> x;
    cin >> y;
    ll sum = (x + y);
    ll avg = (x + y) / 2;

    cout << "The num is = " << avg << endl;

    return 0;
}